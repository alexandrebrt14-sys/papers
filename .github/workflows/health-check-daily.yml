name: Daily health check (papers pipeline)

# Roda 4 horas depois do daily-collect, validando que TUDO esta funcionando.
# Se houver qualquer falha critica, dispara alerta WhatsApp + email.
# Camada redundante: caso o daily-collect falhe silenciosamente, este check pega.

on:
  schedule:
    - cron: '0 13 * * *'  # 13:00 UTC = 10:00 BRT (4h apos daily-collect das 09:00 UTC)
  workflow_dispatch:

concurrency:
  group: papers-health-check
  cancel-in-progress: false

jobs:
  health-check:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: pip install -e .

      # Restore latest papers.db artifact (gerado pelo daily-collect).
      # Aqui usamos dawidd6 propositalmente (workflow_conclusion: success) porque este
      # workflow eh AGENDADO 4h apos o daily-collect e precisa baixar o artifact do
      # ultimo run de daily-collect que terminou com sucesso. Falhamos explicito se
      # nao houver artifact (em vez de warn) para nao mascarar problemas.
      - name: Download papers.db artifact
        uses: dawidd6/action-download-artifact@v6
        with:
          workflow: daily-collect.yml
          workflow_conclusion: success
          name: papers-db-latest
          path: data/
          if_no_artifact_found: fail

      # Garante que o schema esta migrado antes do health_check rodar suas queries.
      - name: Aplicar migracoes do schema
        env:
          PAPERS_DB_PATH: data/papers.db
        run: |
          python -c "from src.db.client import DatabaseClient; c = DatabaseClient(); c.connect(); c.close(); print('schema migrations OK')"

      - name: Run health check
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          GOOGLE_AI_API_KEY: ${{ secrets.GOOGLE_AI_API_KEY }}
          PERPLEXITY_API_KEY: ${{ secrets.PERPLEXITY_API_KEY }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          WHATSAPP_API_TOKEN: ${{ secrets.WHATSAPP_API_TOKEN }}
          WHATSAPP_PHONE_ID: ${{ secrets.WHATSAPP_PHONE_ID }}
          RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
          PAPERS_DB_PATH: data/papers.db
        run: python scripts/health_check.py --min-obs-per-day 200
